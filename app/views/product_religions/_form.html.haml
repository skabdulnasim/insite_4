= form_for @product_religion do |f|
  - if @product_religion.errors.any?
    #error_explanation
      %h2= "#{pluralize(@product_religion.errors.count, "error")} prohibited this product_religion from being saved:"
      %ul
        - @product_religion.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
