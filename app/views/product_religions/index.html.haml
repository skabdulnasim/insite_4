%h1 Listing product_religions

%table
  %tr
    %th Name
    %th
    %th
    %th

  - @product_religions.each do |product_religion|
    %tr
      %td= product_religion.name
      %td= link_to 'Show', product_religion
      %td= link_to 'Edit', edit_product_religion_path(product_religion)
      %td= link_to 'Destroy', product_religion, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Product religion', new_product_religion_path
