= form_for @cost_category do |f|
  - if @cost_category.errors.any?
    #error_explanation
      %h2= "#{pluralize(@cost_category.errors.count, "error")} prohibited this cost_category from being saved:"
      %ul
        - @cost_category.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_area :description
  .actions
    = f.submit 'Save'
