
= render "layouts/flash"
.main-section__content
  .example
    .example__content.p0
      .data-table-container
        %header.data-table-header
          .data-table-header__label
            %span.fs-title Add new SLOT
        = render 'form'

/ %h1 New slot

/ = render 'form'

/ = link_to 'Back', slots_path
