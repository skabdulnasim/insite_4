%h1 Listing printers

%table.table.table-bordered
  %tr
    %th Name
    %th Ip
    %th Assignable
    %th Actions

  - current_user.unit.printers.each do |printer|
    %tr
      %td= printer.name
      %td= printer.ip
      %td= printer.assignable.name
      %td
        = link_to 'Edit', edit_printer_path(printer)
        = link_to 'Destroy', printer, :method => :delete, :data => { :confirm => 'Are you sure?' }