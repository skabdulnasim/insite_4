= form_for @conjugated_unit do |f|
  - if @conjugated_unit.errors.any?
    #error_explanation
      %h2= "#{pluralize(@conjugated_unit.errors.count, "error")} prohibited this conjugated_unit from being saved:"
      %ul
        - @conjugated_unit.errors.full_messages.each do |msg|
          %li= msg

  / .field
  /   = f.label :conjugated_id
  /   = f.number_field :conjugated_id
  .field
    = f.label :conjugated_name
    = f.text_field :conjugated_name
  .actions
    = f.submit 'Save'
