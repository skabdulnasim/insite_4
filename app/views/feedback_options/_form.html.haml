= form_for @feedback_option do |f|
  - if @feedback_option.errors.any?
    #error_explanation
      %h2= "#{pluralize(@feedback_option.errors.count, "error")} prohibited this feedback_option from being saved:"
      %ul
        - @feedback_option.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :option_title
    = f.text_field :option_title
  .actions
    = f.submit 'Save'
